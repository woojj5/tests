# FastAPI 및 웹 서버
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# 머신러닝 프레임워크 (선택적)
# ONNX Runtime 사용 시
# onnxruntime==1.16.3
# onnxruntime-gpu==1.16.3  # GPU 사용 시

# PyTorch 사용 시
# torch==2.1.2
# torchvision==0.16.2

# NumPy (필수)
numpy==1.26.3

# HTTP 클라이언트 (healthcheck용)
requests==2.31.0

# SOC 추정 스크립트용
influxdb-client>=1.38.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# 성능 최적화 (선택적)
# uvloop==0.19.0  # uvicorn과 함께 사용 시 성능 향상

